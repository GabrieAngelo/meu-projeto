git init (git inical)
git add .
git commit -m "Primeiro commit"
git remote add origin + (o codigo ssh do repositorio no github)
git push -u origin main

      se quiser removeer o remote
       git remote -v
       git remote remove origin
       git remote add origin + (o código ssh do repositório no github)
       
  após configurado a primeira vez
   git add .
   git commit -m "Atualização"
   git push     

esse é o passo a passo de como linkar o projeto da máquina, no git hub 




fluxo para trabalhar no projeto
 git clone
↓
git switch develop
↓
git pull
↓
git switch -c feature/minha-funcionalidade
↓
(codar)
↓
git add .
↓
git commit -m "feat: ..."
↓
git push -u origin feature/minha-funcionalidade
↓
Pull Request